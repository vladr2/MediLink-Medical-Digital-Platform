import { TestBed } from '@angular/core/testing';
import { TreatmentsComponent } from './treatments';

describe('TreatmentsComponent', () => {
  it('should be defined as a class', () => {
    expect(TreatmentsComponent).toBeDefined();
  });
});
