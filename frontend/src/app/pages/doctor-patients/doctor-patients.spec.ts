import { TestBed } from '@angular/core/testing';
import { DoctorPatientsComponent } from './doctor-patients';

describe('DoctorPatientsComponent', () => {
  it('should be defined as a class', () => {
    expect(DoctorPatientsComponent).toBeDefined();
  });
});
