import { TestBed } from '@angular/core/testing';
import { AnalysesComponent } from './analyses';

describe('AnalysesComponent', () => {
  it('should be defined as a class', () => {
    expect(AnalysesComponent).toBeDefined();
  });
});
