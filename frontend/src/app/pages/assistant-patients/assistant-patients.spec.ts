import { TestBed } from '@angular/core/testing';
import { AssistantPatientsComponent } from './assistant-patients';

describe('AssistantPatientsComponent', () => {
  it('should be defined as a class', () => {
    expect(AssistantPatientsComponent).toBeDefined();
  });
});
