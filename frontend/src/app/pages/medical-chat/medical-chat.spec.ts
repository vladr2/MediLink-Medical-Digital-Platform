import { TestBed } from '@angular/core/testing';
import { MedicalChatComponent } from './medical-chat';

describe('MedicalChatComponent', () => {
  it('should be defined as a class', () => {
    expect(MedicalChatComponent).toBeDefined();
  });
});
